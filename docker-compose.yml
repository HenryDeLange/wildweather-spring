services:
  wildweather:
    # image: henrydelange/henrydelange/wildweather-spring:latest
    build: .
    container_name: wildweather
    stop_signal: SIGKILL
    tty: true
    restart: unless-stopped
    ports:
      - 8080:8080
    env_file:
      - ./.env.local
